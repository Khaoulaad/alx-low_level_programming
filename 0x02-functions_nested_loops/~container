#include "main.h"

/**
 * _isalpha - check if letter
 *
 * Return: 0 for upper and lowercase 1 else
 * @c : the character to be runned
 */
int _isalpha(int c)
{
if ((c >= 97 && c <= 122) || (c >= 65 && c <= 97))
{
return (1);
}
return (0);
}
