#include "main.h"
#include <stdio.h>

/**
 * _abs - valeur absolue
 *
 * Return: valeur absolue
 *@n : value to be checked
 */
int _abs(int n)
{
if (n > 0)
{
int a_val;
a_val = n;
return (a_val);
}
a_val = n * -1;
return (a_val);
}
