#include "main.h"
#include <stdio.h>

/**
 * add - add two integers
 * @a: value to be added
 * @b: value to be added
 * Return: result
 */
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
