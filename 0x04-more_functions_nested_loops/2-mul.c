#include "main.h"
#include <stdio.h>

/**
 * mul - multiply a and b
 * @a: first number for multiplication
 * @b: second number for multiplication
 * Return: product
 */
int mul(int a, int b)
{
int (product);
product = a * b;
return (product);
}
