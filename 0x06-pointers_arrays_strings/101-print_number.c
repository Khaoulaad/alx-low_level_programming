#include "main.h"

/**
 * print_number - prints a number
 *@n: number to print
 * Return: Always the number
 */

void print_number(int n)
{
unsigned int num;
if (n < 0)
{
num = -n;
_putchar('-');
_putchar(num + '0');
}
if (n > 0)
{
_putchar ('n');
}
}
